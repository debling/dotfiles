IMAPAccount d.ebling8
# Address to connect to
Host imap.gmail.com
User d.ebling8@gmail.com
AuthMechs Login
# To store the password in an encrypted file use PassCmd instead of Pass
PassCmd "cat ~/.local/isync"
#
# Use SSL
SSLType IMAPS
# The following line should work. If get certificate errors, uncomment the two following lines and read the "Troubleshooting" section.
CertificateFile /etc/ssl/certs/ca-certificates.crt

IMAPStore d.ebling8-remote
Account d.ebling8

MaildirStore d.ebling8-local
Subfolders Verbatim
# The trailing "/" is important
Path ~/.mail/d.ebling8/
Inbox ~/.mail/d.ebling8/Inbox

Channel d.ebling8-inbox
Master :d.ebling8-remote:
Slave :d.ebling8-local:
# Exclude everything under the internal [Gmail] folder, except the interesting folders
Patterns * ![Gmail]* "[Gmail]/E-mails enviados" "[Gmail]/Rascunhos" "[Gmail]/Lixeira" "[Gmail]/Todos os e-mails"
# Or include everything
#Patterns *
# Automatically create missing mailboxes, both locally and on the server
Create Both
# Save the synchronization state files in the relevant directory
SyncState *
